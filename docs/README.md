# docs/

Place marketing / screenshot assets used by the StartOS Marketplace here:

- `screenshot-config.png` — the Config action filled in
- `screenshot-stats.png`  — the Show Stats output
- `screenshot-health.png` — the service overview with healthy checks

These are optional and not required for the package to build.
